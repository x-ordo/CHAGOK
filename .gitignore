# ==============================================================================
# 🚨 LEH Project - HYBRID .gitignore
# Security First + Project Specific + Optimized Defaults
# ==============================================================================

# ==============================================================================
# 1. SECURITY & CREDENTIALS (ABSOLUTE BLOCK)
# ==============================================================================
.env
.env.*
!.env.example
*.pem
*.key
*.cert
*.crt
*.p12
secrets.json
credentials.json
*.api_key
*.token
# AWS / Cloud Configs
.aws/
~/.aws/
*.tfvars

# ==============================================================================
# 2. PYTHON (Backend & AI Worker)
# ==============================================================================
# Byte-compiled / Optimized / DLL files
__pycache__/
*.py[cod]
*.class

# Virtual Environments
.venv/
venv/
env/
ENV/
.python-version

# 로그 및 임시 파일
*.log
logs/
tmp/
temp/

###################################
# 2. AI Worker / LLM 파이프라인
###################################
# Storage 데이터 파일만 제외 (Python 코드는 포함)
ai_worker/**/storage/*.db
ai_worker/**/storage/*.sqlite3
ai_worker/**/storage/data/

# 기타 AI Worker 제외 폴더
ai_worker/.cache/
ai_worker/workflows/local_debug/

# 임시 파일 (확장자 기반)
*.tmp
*.api_key
*.token


###################################
# 3. 환경 변수 파일 – 민감 정보 보호
###################################
# 템플릿은 허용(.env.example)
.env
.env.*
!.env.example
# Distribution / Packaging
build/
dist/
*.egg-info/
.eggs/

# Unit Test / Coverage
.pytest_cache/
.coverage
htmlcov/
coverage.xml
coverage/

# Linter / Type Checker Cache
.mypy_cache/
.ruff_cache/
.dmypy.json
dmypy.json

# Jupyter Notebook
.ipynb_checkpoints/

# ==============================================================================
# 3. NODE.JS & NEXT.JS (Frontend)
# ==============================================================================
# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js Build Output
.next/
out/

# Playwright Test Output
playwright-report/
test-results/
playwright/.cache/

# Production Build
build/
dist/

# Playwright
playwright-report/
playwright/.cache/
test-results/

# Debug Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ==============================================================================
# 4. IDE & EDITOR (Collaboration Friendly)
# ==============================================================================
# VS Code - Ignore personal settings, allow shared configs
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains (IntelliJ, PyCharm)
.idea/

# Fleet
.fleet/

# Vim / Emacs / OS
*~
*.swp
*.swo
.DS_Store
Thumbs.db
Desktop.ini

# ==============================================================================
# 5. PROJECT SPECIFIC (LEH) - CAREFULLY SCOPED
# ==============================================================================
# AI Worker Data & Temp
ai_worker/data/
ai_worker/temp/
ai_worker/.cache/

# Evidence Files (Raw uploads)
# Note: We track docs/pdf, so we only ignore raw evidence uploads
evidence_raw/
uploads/

# Docker
docker-data/
*.pid
*.sock

# Agent & Personal Configs
.agent/
.claude/
/CLAUDE.md

# 개인 파일들 담아주세요
.local/

# ==============================================================================
# 6. FILE EXTENSIONS (CAUTION: Only generic binaries/logs)
# ==============================================================================
# Logs & Databases
*.log
*.sqlite3
*.db
*.dump

# Binary / Media (Locally generated)
# Note: PDFs in 'docs/' folders SHOULD be tracked.
# Only ignore these if they are in root or temp folders, but gitignore is recursive.
# Strategy: Ignore specific heavy media folders instead of global extensions if possible.
# For now, keeping global ignore for heavy binaries to prevent accidental commit,
# but using ! to allow specific documentation paths if needed.


###################################
# 11. Personal Local Files
###################################
# Personal CLAUDE.md (prevent accidental commits)
/CLAUDE.md
/claude.md
docs/guides/CLAUDE.md
docs/guides/claude.md

# Personal Claude configuration directory
.claude/
.specify/
.agent/
*.pdf
